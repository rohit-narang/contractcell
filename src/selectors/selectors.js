export const modalOpen = state => state.modal.modalOpen;
export const isModalLoading = state => state.modal.isModalLoading;
export const selectedModal = state => state.modal.selectedModal;
export const modalTitle = state => state.modal.modalTitle;